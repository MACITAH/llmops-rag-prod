# What the app needs to run.

# Claude, with an API key
anthropic>=1.5.0,<2

# PostgreSQL with pgvector: the vector store
psycopg[binary]>=3.2

# Reading the documents
pypdf>=5.0.0
python-docx>=1.1.2

# Splitting text into chunks
langchain-text-splitters>=1.0.0

# The web page
streamlit>=1.50.0
